global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: "task-manager-flask"
    metrics_path: /metrics
    static_configs:
      - targets: ["task-manager:5000"]
